/**
 * Contexts Index
 */

export { SettingsProvider, useSettings, getCurrentSettings, getCurrentCurrency, formatCurrencyValue } from './SettingsContext';
export type { Language, Currency } from './SettingsContext';
